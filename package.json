{
  "name": "@vlindersoftware/generate-licenses",
  "version": "1.2.11",
  "description": "GitHub Action for generating comprehensive open source license reports from npm dependencies",
  "main": "action.yml",
  "bin": {
    "generate-licenses-csv": "./scripts/generate-licenses-csv.cjs",
    "download-licenses": "./scripts/download-licenses.cjs",
    "generate-licenses-html": "./scripts/generate-licenses-html.cjs"
  },
  "files": [
    "scripts/",
    "templates/",
    "dist/",
    "action.yml",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "ncc build scripts/generate-licenses-csv.cjs -o dist/csv && ncc build scripts/download-licenses.cjs -o dist/download && ncc build scripts/generate-licenses-html.cjs -o dist/html",
    "test": "jest",
    "test:unit": "jest --testPathIgnorePatterns=integration",
    "test:integration": "jest --config jest.integration.config.js",
    "test:all": "npm run test:unit && npm run test:integration",
    "test:watch": "jest --watch --testPathIgnorePatterns=integration",
    "test:coverage": "jest --coverage --testPathIgnorePatterns=integration",
    "licenses:csv": "node scripts/generate-licenses-csv.cjs",
    "licenses:download": "node scripts/download-licenses.cjs",
    "licenses:html": "node scripts/generate-licenses-html.cjs",
    "licenses:generate": "npm run licenses:csv && npm run licenses:download && npm run licenses:html"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vln-devsecops/actions-generate-licenses.git"
  },
  "keywords": [
    "github-action",
    "licenses",
    "npm",
    "dependencies",
    "compliance",
    "open-source"
  ],
  "author": "VlinderSoftware",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/vln-devsecops/actions-generate-licenses/issues"
  },
  "homepage": "https://github.com/vln-devsecops/actions-generate-licenses#readme",
  "devDependencies": {
    "@vercel/ncc": "^0.45.0",
    "jest": "^30.4.2",
    "js-yaml": "^4.1.0"
  },
  "dependencies": {
    "csv-parser": "^3.2.0",
    "js-yaml": "^4.1.0",
    "nunjucks": "^3.2.4"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  }
}
