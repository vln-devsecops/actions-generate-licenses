name: 'Generate Licenses'
description: 'Generate license CSV, download license texts with caching, and create customizable HTML page'

inputs:
  working-directory:
    description: 'Directory containing package.json (e.g., frontend)'
    required: true
  node-version:
    description: 'Node.js version to use'
    required: false
    default: '22'
  fail-on-unknown:
    description: 'Whether to fail the build if unknown licenses are detected'
    required: false
    default: 'false'
  fail-on-copyleft:
    description: 'Whether to fail the build if copyleft licenses are detected'
    required: false
    default: 'false'
  production-only:
    description: 'Whether to only check production dependencies (excludes devDependencies)'
    required: false
    default: 'false'
  fail-on-missing-licenses:
    description: 'Whether to fail the build if license files cannot be downloaded'
    required: false
    default: 'true'

runs:
  using: 'composite'
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v7
      with:
        node-version: ${{ inputs.node-version }}

    - name: Generate licenses CSV
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: node ${{ github.action_path }}/dist/csv/index.cjs
      env:
        FAIL_ON_UNKNOWN: ${{ inputs.fail-on-unknown }}
        FAIL_ON_COPYLEFT: ${{ inputs.fail-on-copyleft }}
        PRODUCTION_ONLY: ${{ inputs.production-only }}

    - name: Create cache key from CSV
      id: cache-key
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        # Create a hash of the CSV file to use as cache key
        CSV_HASH=$(sha256sum licenses/licenses.csv | cut -d' ' -f1 | cut -c1-8)
        echo "csv-hash=${CSV_HASH}" >> $GITHUB_OUTPUT
        echo "Cache key will be: licenses-${CSV_HASH}"

    - name: Restore cached license files
      id: cache-licenses
      uses: actions/cache@v5
      with:
        path: ${{ inputs.working-directory }}/licenses/texts
        key: licenses-${{ steps.cache-key.outputs.csv-hash }}
        restore-keys: |
          licenses-

    - name: Download license files
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      env:
        FAIL_ON_MISSING_LICENSES: ${{ inputs.fail-on-missing-licenses }}
      run: node ${{ github.action_path }}/dist/download/index.cjs

    - name: Generate licenses HTML
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: node ${{ github.action_path }}/dist/html/index.cjs

    - name: Display license summary
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: |
        echo "📊 License Generation Summary:"
        echo "=============================="
        if [ -f licenses/licenses.csv ]; then
          LICENSE_COUNT=$(tail -n +2 licenses/licenses.csv | wc -l)
          echo "Total packages: ${LICENSE_COUNT}"
        fi
        if [ -d licenses/texts ]; then
          FILE_COUNT=$(find licenses/texts -type f | wc -l)
          echo "Downloaded license files: ${FILE_COUNT}"
        fi
        if [ -f public/licenses.html ]; then
          echo "✓ Generated public/licenses.html"
        fi
